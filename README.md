# OpenCV Project Template

## Quick Start

```shell
mkdir build && cd build
cmake ..
cmake --build .
```

## Dependencies

Windows:

```cmd
.\setup_dependencies.bat
```

Linux (Ubuntu/Debian):

```shell
sudo apt install libopencv-dev
```

MacOS:

```shell
brew install opencv
```
